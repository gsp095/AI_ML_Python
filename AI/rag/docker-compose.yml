version: '3.8'

services:
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      # Map a local folder named "qdrant_storage" to Qdrant's internal storage path
      - ./qdrant_storage:/qdrant/storage